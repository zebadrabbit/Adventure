# Adventure MUD - Environment Variables Example
#
# Copy this file to .env and fill in the appropriate values for your environment.
# Lines starting with '#' are comments.
#
# You can override these on the command line with flags, e.g.:
#   python run.py --env-file .env server --host 127.0.0.1 --port 8080
#
# SECURITY NOTE:
# - Never commit your real SECRET_KEY or production database credentials.
# - This example uses development defaults.

# Flask secret key (used for session signing)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-with-a-secure-random-hex

# Server bind address and port
# Typical choices: 0.0.0.0 (all interfaces) or 127.0.0.1 (localhost only)
HOST=0.0.0.0
PORT=5000

# SQLAlchemy database URI
# Default dev DB lives under the Flask instance folder: ./instance/mud.db
# If unset, the app will construct an absolute sqlite path automatically.
# If you set it explicitly, prefer an absolute path for sqlite URIs to avoid
# "unable to open database file" errors. Examples:
#   sqlite:////absolute/path/to/instance/mud.db
#   postgresql+psycopg://user:pass@localhost:5432/adventure
#   mysql+pymysql://user:pass@localhost:3306/adventure
# DATABASE_URL=sqlite:////absolute/path/to/instance/mud.db

# CORS settings for Socket.IO
# Comma-separated origins or '*' for all (dev only). Be explicit in production.
CORS_ALLOWED_ORIGINS=*
